/**
 * PurgeCSS configuration for Model Lens Dashboard.
 *
 * Runs after `astro build` in the deploy pipeline to remove unused CSS
 * selectors from the bundled CSS files. Scans built JS/HTML for class
 * usage and strips any CSS rules that aren't referenced.
 *
 * The safelist regex patterns catch dynamically-constructed class names
 * (e.g., `${run.status}` → traces-status-completed) that static analysis
 * can't resolve at build time.
 */
module.exports = {
  // Scan all built client and server assets for CSS selector usage
  content: [
    "./dist/client/**/*.html",
    "./dist/client/**/*.js",
    "./dist/server/**/*.html",
    "./dist/server/**/*.js",
  ],
  css: ["./dist/client/**/*.css"],
  // Keep selectors matching these patterns (dynamic class names)
  safelist: {
    standard: [
      /^traces-status-/,
      /^traces-step-/,
      /^traces-tab-/,
      /^traces-filter-/,
      /^traces-run-/,
      /^traces-tl-/,
      /^traces-pb-/,
      /^traces-shortcuts-/,
      /^traces-kbd-/,
      /^traces-tool-/,
      /^traces-response-/,
      /^traces-toast-/,
      /^traces-sidebar-/,
      /^traces-playback-/,
      /^traces-step-/,
      /^traces-layout-/,
      /^tier-/,
      /^chip-/,
      /^heatmap-/,
      /^metric-/,
      /^pack-/,
      /^rpv-/,
      /^lb-/,
      /^tcmp-/,
      /^status-/,
      /^node-status/,
      /^pg-/,
      /^sg-/,
      /^wl-/,
      /^hist-/,
      /^lbp-/,
      /^connection-/,
      /^server-uptime/,
      /^diff-/,
      /^theme-toggle/,
      /^hamburger-/,
      /^menu-/,
      /^search-/,
      /^topbar-/,
      /^sidebar-/,
      /^brand-/,
      /^btn-/,
      /^card-/,
      /^grid-/,
      /^col-span-/,
      /^empty-state/,
      /^chart-/,
      /^legend-/,
      /^ctx-/,
      /^table-/,
      /^sort-/,
      /^dashboard-/,
      /^resource-/,
      /^trace-stat-/,
      /^skeleton-/,
      /^animate-/,
      /^model-/,
      /^perf-/,
      /^compare-/,
      /^cell-/,
      /^metric-card/,
      /^page-hero/,
      /^back-link/,
      /^radar-/,
      /^speed-/,
      /^fail-/,
      /^score-/,
      /^overall-/,
      /^rank-/,
      /^row-/,
      /^best-/,
      /^confidence-/,
      /^stats-/,
      /^meta-/,
      /^mono-/,
      /^section-/,
      /^replay-/,
      /^replays-/,
      /^dot-play/,
      /^step-arrive/,
      /^slide-in/,
      /^shortcuts-/,
      /^status-/,
      /^status-progress/,
      /^status-dot/,
      /^status-text/,
      /^status-kill/,
      /^status-flyout/,
      /^status-shimmer/,
      /^pulse-/,
      /^spin-/,
      /^toast-/,
      // Animation keyframes referenced by class names
      /^anim-/,
    ],
    // Greedy catches ANY selector containing these patterns
    greedy: [
      /material-symbols/,
      /keyframes/,
      /transition/,
      /@keyframes/,
    ],
    deep: [
      /keyframes/,
    ],
  },
};
